[project]
name = "experiment-and-simulation-logbook"
version = "0.1.0"
description = "A local Python application for documenting and managing experiment and simulation runs."
requires-python = ">=3.14"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py314"
line-length = 88
src = ["src", "tests"]